import { helpful } from 'qanda-messageboard/helpers/helpful';
import { module, test } from 'qunit';

module('Unit | Helper | helpful');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = helpful([42]);
  assert.ok(result);
});
